# No third-party dependencies. Everything here runs on the Python 3.9+ standard
# library so it works in any checkout without a virtualenv.
#
#   python3 research/tools/decode.py research/captures/ --layout
#   python3 -m unittest discover -s research/tools -p 'test_*.py'
